# Project Instructions

- 오류 메시지와 사용자의 설명을 먼저 문자 그대로 해석한다.
- 오류가 "사망 후 이동"을 뜻하면, 물리·충돌 설정을 추측하기 전에 실제 이동 명령을 내리는 주체(예: Behavior Tree의 `Move To`)와 사망 상태 검사 여부를 먼저 확인한다.
- 스크린샷에서 확인되지 않은 노드, 연결, 실행 순서 또는 원인을 사실처럼 단정하지 않는다.
- 확인된 사실과 추정은 명확히 구분하며, 원인을 제시하기 전에 로그·스크린샷·실행 경로로 검증한다.
- 사용자가 이미 정상이라고 확인한 설정은 다시 문제로 지목하지 않는다.
- Unreal Engine UI를 안내할 때는 화면에 표시되는 노드명과 핀명을 그대로 사용한다.
- 모든 사용자 대상 최종 답변의 마지막 줄에 `누적 실수: N회`를 표시한다. 현재 누적 기준은 54회이며, 이후 새로 확인된 실수가 생길 때마다 1씩 올린다.
- 사용자가 제공한 정보만으로 진행 가능한 상황에서는 사용자에게 원인이나 해결책을 되묻지 않는다. 먼저 로그, 스크린샷, 프로젝트 설정과 실행 경로를 직접 확인하고 해결안을 제시한다.
- 문제 해결 요청에는 확인·사과·상태 보고만 단답으로 남기지 않는다. 모든 최종 답변에 실행 가능한 해결 방법을 반드시 포함한다.
- 사용자가 이미 적용했거나 정상이라고 확인한 노드, 핀, 설정은 의심하거나 다시 확인시키지 않는다. 동일 설정을 반복 안내하지 않는다.
- 사용자의 최신 스크린샷과 설명을 이전 추측보다 우선한다. 화면에서 확인된 잘못된 값이나 연결을 정확한 UI 용어로 지적하고 바로 수정값을 제시한다.
